{
  "name": "uni-verse",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "node --test",
    "start": "http-server -c-1",
    "build": "rm -rf dist && mkdir dist && esbuild main.js --bundle --outfile=dist/bundle.js --minify --sourcemap && node scripts/build-html.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "rot-js": "^2.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "http-server": "^14.1.1"
  }
}
